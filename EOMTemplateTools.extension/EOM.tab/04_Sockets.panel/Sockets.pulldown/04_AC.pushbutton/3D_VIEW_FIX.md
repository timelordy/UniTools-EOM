# ✅ ИСПРАВЛЕНО - 3D вид отладки со стенами

**Дата**: 2026-01-09 12:15
**Проблема**: Вид отладки показывал только потолок (plan view)
**Решение**: Изменен на 3D вид с section box

## 🐛 Проблема

Старая функция `_open_plan_check_view()` создавала **вид в плане** (plan view):
- ❌ Показывал только потолок (вид сверху)
- ❌ Не видно стен
- ❌ Не видно на какой стене розетка
- ❌ Сложно проверить правильность размещения

## ✅ Решение

### Создается 3D вид с section box:

**Было:**
```python
def _open_plan_check_view(..., view_name='Plan_Check_AC_Sockets'):
    # Создавал ViewPlan (вид в плане)
    vplan = DB.ViewPlan.Create(doc, vft.Id, lvl_lo.Id)
    # Настраивал ViewRange (cut plane, top plane, etc.)
```

**Стало:**
```python
def _open_plan_check_view(..., view_name='3D_Check_AC_Sockets'):
    """Open 3D view with section box to show walls and sockets."""
    # Создает View3D (3D вид)
    v3d = DB.View3D.CreateIsometric(doc, vft.Id)

    # Настраивает section box
    v3d.IsSectionBoxActive = True
    sb = DB.BoundingBoxXYZ()
    sb.Min = XYZ(min_x - pad, min_y - pad, z_floor)
    sb.Max = XYZ(max_x + pad, max_y + pad, z_ceiling)
    v3d.SetSectionBox(sb)

    # Улучшает визуализацию
    v3d.DisplayStyle = DisplayStyle.ShadingWithEdges
    v3d.DetailLevel = ViewDetailLevel.Fine
```

## 🎨 Что показывает новый вид

### Старый вид (Plan):
```
┌─────────────────┐
│                 │  ← потолок (вид сверху)
│    ● розетки    │
│                 │
└─────────────────┘
```
❌ Не видно стен, только точки розеток на плане

### Новый вид (3D с section box):
```
    ╔═══════════════╗
   ╔╝ ┌───────────┐ ╚╗
  ╔╝  │           │  ╚╗ ← потолок
 ╔╝   │           │   ╚╗
╔╝  ● │  розетка  │    ╚╗ ← стена
║     │    на     │     ║
║     │   стене   │     ║
╚╗    │           │    ╔╝
 ╚╗   └───────────┘   ╔╝ ← пол
  ╚╗                  ╔╝
   ╚══════════════════╝
```
✅ Видно:
- Стены (вертикальные)
- Пол и потолок
- Розетки на стенах
- Их положение в углу

## 📊 Параметры 3D вида

### Section Box:
- **Min XYZ**: `(min_x - pad, min_y - pad, z_floor)`
- **Max XYZ**: `(max_x + pad, max_y + pad, z_ceiling)`
- **Padding**: 3 фута (≈900 мм) вокруг элементов
- **Z range**: от пола до потолка

### Display Settings:
- **DisplayStyle**: `ShadingWithEdges` (с тенями и рёбрами)
- **DetailLevel**: `Fine` (максимальная детализация)
- **IsSectionBoxActive**: `True` (включен section box)

## 🔍 Что можно увидеть

После размещения розеток нажмите **"Открыть (3D вид)"**:

### В 3D виде вы увидите:
1. ✅ **Стены** (вертикальные поверхности)
2. ✅ **Пол** (нижняя граница)
3. ✅ **Потолок** (верхняя граница)
4. ✅ **Розетки на стенах** (с их реальным положением)
5. ✅ **Углы помещений** (где должны быть розетки)

### Можно проверить:
- ✅ Розетка НЕ на фасаде (не на внешней стене)
- ✅ Розетка на ПЕРПЕНДИКУЛЯРНОЙ стене
- ✅ Розетка В УГЛУ
- ✅ Розетка на нужной ВЫСОТЕ (300 мм от потолка)
- ✅ Розетка БЛИЗКО к корзине

## 🎯 Как пользоваться

### 1. После размещения розеток:
Скрипт покажет диалог:
```
Связей обработано: 1
Найдено корзин/блоков: 12
Создано розеток: 8

Открыть 3D вид для проверки?
[Открыть (3D вид)]  [Закрыть]
```

### 2. Нажмите "Открыть (3D вид)":
- Создастся/откроется вид **"3D_Check_AC_Sockets"**
- Активируется section box вокруг розеток
- Выделятся все размещенные розетки

### 3. В 3D виде:
- Вращайте вид (средняя кнопка мыши)
- Увеличивайте/уменьшайте (колесико)
- Проверяйте положение розеток на стенах

### 4. Настройка вида:
Можно вручную:
- Вращать section box (grip points на рёбрах)
- Изменить DisplayStyle (Properties → Visual Style)
- Включить/выключить категории (VV)

## 📝 Изменения в коде

### Изменены строки:
1. **Строка 425**: Название функции и view_name
   - `'Plan_Check_AC_Sockets'` → `'3D_Check_AC_Sockets'`

2. **Строки 477-563**: Полная замена логики
   - Удалено создание ViewPlan
   - Добавлено создание View3D.CreateIsometric
   - Добавлена настройка section box
   - Добавлены DisplayStyle и DetailLevel

3. **Строка 4635**: Текст кнопки
   - `"Открыть (План)"` → `"Открыть (3D вид)"`

4. **Строка 4638**: Условие проверки
   - `if choice == "Открыть (План)"`
   - → `if choice == "Открыть (3D вид)"`

5. **Строка 4643**: Сообщение об ошибке
   - `"Не удалось открыть план"`
   - → `"Не удалось открыть 3D вид"`

## 🚀 Дополнительные возможности

### В 3D виде можно:
- **Измерить расстояния** (Measure)
- **Проверить уровни** (показать level symbols)
- **Посмотреть связи** (включить linked models)
- **Сделать скриншот** (для документации)

### Сохранение вида:
3D вид сохраняется в проекте как **"3D_Check_AC_Sockets"**.
При повторном запуске:
- Если вид существует → переиспользуется
- Обновляется section box под новые элементы

## ✅ Результат

**До:**
- Вид в плане (сверху)
- Видны только точки розеток
- Не понятно на какой стене

**После:**
- 3D вид с section box
- Видны стены, пол, потолок
- Видно точное положение розеток на стенах
- Легко проверить правильность размещения

---

**Статус**: 🟢 ГОТОВО

Теперь после размещения розеток вы сможете увидеть их в 3D виде со стенами!
